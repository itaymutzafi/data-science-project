name: ds-project
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy
  - pandas
  - matplotlib
  - seaborn
  - scikit-learn
  - scipy
  - jupyter
  - ipykernel
  - pip
  - pip:
      - yfinance==0.2.37
      - tqdm==4.66.2
      - jupyterlab==4.0.0
      -  # Optional / team additions (uncomment when needed): shap==0.45.0
